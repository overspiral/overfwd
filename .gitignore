# Rust
/target
**/*.rs.bk
*.pdb

# Local environment (per-worktree, seeded from .env.example)
.env
.env.local

# Local secrets / scratch credentials (never commit)
pass.txt

# Editor / OS
.idea/
.vscode/
.DS_Store
